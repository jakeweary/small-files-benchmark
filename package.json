{
  "name": "small-files-benchmark",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "setup": "mkdir -p small-files && for i in $(seq 1 20000); do echo This is file $i > small-files/$i.txt; done",
    "bench": "node bench"
  },
  "dependencies": {
    "bluebird": "^3.5.2"
  }
}
